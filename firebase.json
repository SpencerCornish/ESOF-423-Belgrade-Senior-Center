{
  "firestore": {
    "rules": "firebase_rulesets/firestore.rules",
    "indexes": "firebase_rulesets/firestore.indexes.json"
  },
  "hosting": {
    "public": "build",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  },
  "storage": {
    "rules": "firebase_rulesets/storage.rules"
  }
}
